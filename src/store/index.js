import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = {}
const getter = {}
const mutations = {}
const actives = {}

export default new Vuex.Store({
  state,
  getter,
  mutations,
  actives
})
